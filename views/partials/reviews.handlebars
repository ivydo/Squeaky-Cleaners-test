<div class="reviews">
    {{!-- {{#each this}} --}}
    <section class="review">
        <div class="meta">
            {{ title }} on {{format_date created_at}}
        </div>

        <div class="text">
            
            {{review_text}} 
            {{!-- {{user.username}} --}}
        </div>
    </section>
    {{!-- {{/each}} --}}
</div>